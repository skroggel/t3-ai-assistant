..  _data-connections:

Connection records
==================

AI connection records configure chat and embedding providers. Vector store
connection records configure vector databases.

Connection records are referenced by assistant profiles and indexer
configurations. This allows multiple assistants and indexers to reuse the same
provider settings.

